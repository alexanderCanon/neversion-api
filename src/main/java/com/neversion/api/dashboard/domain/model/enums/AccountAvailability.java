package com.neversion.api.dashboard.domain.model.enums;

/**
 * Calculated availability of an account — never persisted (BR-06).
 */
public enum AccountAvailability {
    PARTIAL,
    NO_AVAILABILITY,
    INDIVIDUAL,
    COMPLETE
}
