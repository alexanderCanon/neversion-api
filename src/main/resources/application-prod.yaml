spring:
  flyway:
    baseline-on-migrate: true
    baseline-version: 0

logging:
  structured:
    format:
      console: ecs

springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false